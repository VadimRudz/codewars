pipeline {
    agent any

    stages {
        stage('Cat test') {
            steps {
                sh 'node --version'
                sh 'npm --version'
                sh 'npm install'
                sh 'npm run cat'
            }
        }
    }
}